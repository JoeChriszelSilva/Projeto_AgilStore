export default class Product {
  constructor(id, nome, categoria, quantidade, preco) {
    this.id = id;
    this.nome = nome;
    this.categoria = categoria;
    this.quantidade = quantidade;
    this.preco = preco;
  }
}
